# dependencies
node_modules/

# next.js
.next/
out/

# production
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (pode ser versionado seletivamente se precisar)
.env*

# vercel
.vercel

# firebase (cache local de deploy e logs)
.firebase/
firebase-debug.log*
firestore-debug.log*
ui-debug.log*

# firebase functions (build)
functions/lib/

# typescript
*.tsbuildinfo
next-env.d.ts

# Token de acesso da Meta — nunca versionado
functions/.meta-token
functions/.app-secret

# Permissões locais do Claude Code — decisão de confiança pessoal, não do repo
.claude/settings.local.json
