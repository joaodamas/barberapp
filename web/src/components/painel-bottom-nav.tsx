"use client";

import { BottomNav } from "@/components/bottom-nav";
import { painelNavItems } from "@/lib/nav-items";

export function PainelBottomNav() {
  return <BottomNav items={painelNavItems} />;
}
